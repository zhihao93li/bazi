{
  "$schema": "https://schema.zeabur.app/v1.json",
  "build_command": "npx prisma generate && npx prisma db push --skip-generate --accept-data-loss && npm run build",
  "start_command": "npm start",
  "output_dir": "dist"
}