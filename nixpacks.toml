# Nixpacks 配置 - 后端 API 服务

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["rm -rf node_modules/.cache || true", "npm ci --cache /tmp/.npm"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npx prisma migrate deploy && npm start"
